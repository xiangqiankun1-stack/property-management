server:
  port: 8080

spring:
  application:
    name: property-management
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart_reader
    username: root
    password: 050223ljl

logging:
  level:
    root: info
    com.smart.reader: debug